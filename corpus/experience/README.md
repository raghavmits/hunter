# experience/

One file per role held: scope, impact, metrics (PLAN.md FR-16).

One role per file — not one company per file, if you held multiple roles at the same company. Name the file after the role and company (e.g. `2022-senior-swe-acme.md`) so the list sorts usefully on its own.

Start a new role file from [`template.md`](template.md).
